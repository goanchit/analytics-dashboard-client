import { styled } from '@mui/system';

export const DropDownHolder = styled('div')({
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
});

export const DialogBoxPadding = styled('div')({
    padding: '3rem',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
});

export const PaddingButton = styled('div')({
    padding: '2rem',
});